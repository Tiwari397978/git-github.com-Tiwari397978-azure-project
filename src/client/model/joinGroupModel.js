'use strict'

export async function joinGroup (group) {
  console.log(group)
/*   try {
    const response = await fetch('/group/api/request') // Returns a Promise for the GET request
    // Check if the request returned a valid code
    if (!response.ok) {
      throw new Error('Failed to connect to search api and log request')
    }
    // Return the response parse as JSON if code is valid
    return await response.json()
  } catch (e) {
    console.log(e)
  } */
}
